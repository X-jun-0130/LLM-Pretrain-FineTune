# ==================== No_Solution: GenRM 选择模式 ====================
SELECTION_PROMPT_TEMPLATE_NO_SOLUTION = '''
你是一名严谨的医学专家，正在对多个 AI 生成的候选回答进行同行评议，从多个候选答案中选出**最优答案**。

**任务：同行评议与最佳答案遴选**
    - **零容忍原则**：任何包含危险医疗建议、严重术语错误或逻辑断裂的回答必须直接淘汰。
    - **严禁根据答案长度进行判断**。短小精悍的回答可能优于冗长空洞的回答。

**评估维度（请综合考量）**
1.  **答案准确性**：评估答案是否合理、符合用户意图。
2.  **逻辑严密性**：推理链条是否完整？前后是否存在矛盾？是否回答了用户问题的核心诉求？
3.  **语言表达**：评估答案的语言表达是否流畅自然、是否存在不当的语言混杂、格式是否清晰可控。

<评估信息>
**对话历史**（上下文信息，按时间顺序排列，从最早到最新）
```
{插入对话历史}
```

**当前问题**（用户提出的具体请求）
```
{插入原始问题}
```

**知识核查清单**
```
### 核心知识（均来自权威书籍与指南）
{插入核心知识}
```

**候选答案列表**（共 {n_candidates} 个候选答案）
{candidates_block}
</评估信息>


**输出格式**
请进行对比分析，选出最优答案，当最优答案有多个时，请返回所有最优答案的序号。
**注意：必须仅输出对应的数字序号列表，严禁输出答案文本内容。**
将结果严格置于 `\\boxed{[选中的答案序号列表]}` 中，如 \\boxed{[1,3,5]}。
若所有答案质量均差，请返回 \\boxed{[]}。请勿返回其他内容。
'''.strip()



# ==================== With_Solution: GenRM 选择模式 ====================
SELECTION_PROMPT_TEMPLATE_WITH_SOLUTION = '''
你是一名严谨的医学专家，正在对多个 AI 生成的候选回答进行同行评议，从多个候选答案中选出**正确答案**。

**任务：同行评议与正确答案遴选**
    - **零容忍原则**：任何包含危险医疗建议、严重术语错误或逻辑断裂的回答必须直接淘汰。

**评估维度（请综合考量）**
1.  **答案准确性**：评估答案是否合理、符合用户意图。
2.  **逻辑严密性**：推理链条是否完整？前后是否存在矛盾？是否回答了用户问题的核心诉求？
3.  **语言表达**：评估答案的语言表达是否流畅自然、是否存在不当的语言混杂、格式是否清晰可控。

<评估信息>
**对话历史**（上下文信息，按时间顺序排列，从最早到最新）
```
{插入对话历史}
```

**当前问题**（用户提出的具体请求）
```
{插入原始问题}
```

**知识核查清单**
```
### 参考答案（作为当前问题的正确结果，可能不包含完整的解题过程）
{插入优秀答案}


### 核心知识（均来自权威书籍与指南）
{插入核心知识}
```

**候选答案列表**（共 {n_candidates} 个候选答案）
{candidates_block}
</评估信息>


**输出格式**
请结合**优秀答案**进行对比分析，选出正确答案，当正确答案有多个时，请返回所有正确答案的序号。
**注意：必须仅输出对应的数字序号列表，严禁输出答案文本内容。**
将结果严格置于 `\\boxed{[选中的答案序号列表]}` 中，如 \\boxed{[1,3,5]}。
若所有答案质量均差，请返回 \\boxed{[]}。请勿返回其他内容。
'''.strip()


