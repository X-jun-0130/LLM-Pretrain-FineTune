# -*- coding: utf-8 -*-
"""
提示词模块 - 指令多样化合成（v2 - 适配结构化版本管理格式）

核心设计原则：
1. 基于结构化版本JSON（task_prompt + output_formate）进行指令改写
2. {medical_text} 占位符绝对不可变
3. output_formate 绝对不可变（不参与改写）
4. task_prompt 中的医学业务规则不可变意思，可变形式
"""

# ============================================================
# 指令改写提示词
# ============================================================

REWRITE_SYSTEM_PROMPT = """你是一位专业的医疗AI指令改写专家。你的任务是对医疗任务指令（task_prompt）进行**表达形式多样化改写**，同时**严格保留所有医学业务逻辑和规则含义**。

## 铁律（违反任何一条即为失败）

1. **占位符 `{medical_text}` 不可变**：指令中的 `{medical_text}` 是后续输入医疗文本的占位符，必须原封不动保留，不可修改、删除或拆分。如果原文中没有此占位符也不要自行添加
2. **医学知识不可变**：所有医学术语、诊断规则、错误类型分类、质控原则、豁免清单的具体内容必须100%保留
3. **评判维度不可变**：所有用于评估/比对/分析的维度列表必须完整保留，不可增删
4. **限制条件不可变**：所有"禁止"、"不处理"、"仅处理"、"忽视"、"豁免"等约束条件必须完整保留
5. **医学示例不可变**：所有用于解释规则的医学举例（如 `纵隔`、`Crohn`、`心影增` 等）必须保留
6. **分析步骤逻辑不可变**：步骤之间的先后因果关系必须保留，但步骤的表述方式可以改变
7. **内嵌格式说明不改写**：如果指令中内嵌了输出格式说明或JSON示例，必须原封不动保留
8. **不可发明新的输出格式**：不可在改写结果中自行添加、发明或修改输出格式要求（如JSON示例、代码块格式说明）。该任务指令的输出格式由独立的 output_formate 字段管理，改写后的 task_prompt 中关于格式的代码块数量、结构、字段名必须与原文完全一致，不得新增、删除或修改任何格式块

## 你可以改变的部分

1. **文档结构**：标题层级（## / ### / 无标题）、编号方式（1./a./i.）、段落划分、是否使用markdown
2. **措辞风格**：正式/半正式、指令式/引导式、简洁/详尽
3. **呈现顺序**：在不影响逻辑的前提下调整段落排列（如先说任务目标再说规则，或反过来）
4. **角色设定**：可以改变角色描述（如"作为影像质控专家"改为"你需要扮演一位严格的报告审核员"）
5. **过渡语与引导语**：可以增减连接性文字，让指令更流畅或更精炼
6. **格式化细节**：加粗/斜体/引用块等markdown格式的使用方式

## 适应性要求

改写后的指令应该像是一位不同背景的医疗专家从零编写的同一任务指令——
- 有时像规范文档，有时像对话式指导，有时像步骤化手册
- 有时详细展开每条规则，有时精炼概括
- 有时使用标准markdown结构，有时使用纯文本叙述
- 但无论形式如何变化，传达的医学规则和业务逻辑必须完全一致"""


REWRITE_USER_PROMPT = """请对以下医疗任务指令进行表达多样化改写。

## 改写强度：{rewrite_level}

{rewrite_guidance}

## 原始 task_prompt

```
{task_prompt}
```
{output_formate_section}
## 改写注意事项

1. `{{medical_text}}` 是文本输入占位符，如果原文包含则必须原封不动保留在改写结果中
2. 直接输出改写后的完整 task_prompt 文本
3. 不要包含任何解释、元信息
4. 不要用 ``` 代码块包裹输出
5. 改写结果必须可以直接替换原始 task_prompt 使用
6. **绝对禁止**自行发明新的输出格式 —— 如果原始 task_prompt 中有格式说明/JSON示例/代码块，保持原样不动；如果原文中没有，也绝对不可自行添加"""


# ============================================================
# 改写强度定义
# ============================================================

REWRITE_LEVELS = {
    "light": {
        "name": "轻度改写",
        "guidance": """- 保持整体结构不变，仅改变措辞表达
- 替换同义词和短语，调整句式（主动↔被动）
- 微调标题和编号格式
- 可以适当增删过渡句"""
    },
    "medium": {
        "name": "中度改写",
        "guidance": """- 可以重新组织段落结构和信息呈现顺序
- 可以将列表改为段落叙述，或反过来
- 可以调整markdown格式的使用方式（加粗/标题/引用等）
- 可以改变角色设定的措辞
- 可以将详细说明精简为要点，或将要点展开为说明"""
    },
    "heavy": {
        "name": "重度改写",
        "guidance": """- 大幅重构文档组织方式（如平铺↔层级、文档式↔对话式↔步骤式）
- 可以完全打乱重排信息顺序
- 可以去掉所有markdown格式改为纯文本，或反过来增加丰富格式
- 可以增加额外解释性文字让规则更易理解
- 核心约束：形式大变，但每一条医学规则必须完整保留"""
    }
}


# ============================================================
# 语义一致性校验提示词
# ============================================================

VERIFY_SYSTEM_PROMPT = """你是一位严格的医疗指令质量审核专家。你的任务是对比原始 task_prompt 和改写后的 task_prompt，判断改写是否保留了所有关键业务逻辑。"""


VERIFY_USER_PROMPT = """请逐项对比以下原始指令和改写后的指令，检查改写是否安全。

## 原始 task_prompt

```
{original_prompt}
```

## 改写后的 task_prompt

```
{rewritten_prompt}
```

## 检查清单

请逐项检查以下维度，对每个维度给出"通过"或"不通过"的判定：

1. **占位符完整性**：`{{medical_text}}` 占位符是否原封不动保留？（如果原文包含的话）
2. **医学术语完整性**：所有医学专业术语是否完整保留？
3. **错误类型/评判维度完整性**：所有分类项是否一项不少？
4. **质控原则/限制条件完整性**：所有原则和限制是否完整保留，语义未被扭曲？
5. **医学示例完整性**：所有用于说明规则的医学举例是否保留？
6. **分析步骤完整性**：分析步骤的逻辑关系是否保留？
7. **无新增约束**：改写是否引入了原始指令中不存在的新规则或新限制？
8. **无语义偏移**：改写是否改变了任何规则的含义？
9. **无格式冲突**：改写后的 task_prompt 中是否自行发明了原始指令中不存在的输出格式要求（如JSON示例、代码块）？格式块数量和内容是否与原文一致？

## 输出格式

```json
{{
  "pass": true/false,
  "checks": {{
    "占位符完整性": {{"pass": true/false, "issue": "问题描述（通过则为空）"}},
    "医学术语完整性": {{"pass": true/false, "issue": ""}},
    "错误类型完整性": {{"pass": true/false, "issue": ""}},
    "质控原则完整性": {{"pass": true/false, "issue": ""}},
    "医学示例完整性": {{"pass": true/false, "issue": ""}},
    "分析步骤完整性": {{"pass": true/false, "issue": ""}},
    "无新增约束": {{"pass": true/false, "issue": ""}},
    "无语义偏移": {{"pass": true/false, "issue": ""}},
    "无格式冲突": {{"pass": true/false, "issue": ""}}
  }},
  "summary": "总体评价"
}}
```"""
